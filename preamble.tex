% Package imports
\usepackage{amssymb,amsmath,amsthm,amsfonts}
\usepackage{multicol,multirow}
\usepackage{spverbatim}
\usepackage{graphicx}
\usepackage{ifthen}
\usepackage{enumitem}
\usepackage[landscape]{geometry}
\usepackage[colorlinks=true,urlcolor=olgreen]{hyperref}
\usepackage{booktabs}
% \usepackage{fontspec}
\usepackage[vietnamese]{babel}
\usepackage{microtype}
\usepackage{academicons}
\usepackage{xcolor}
\usepackage{titlesec}
\usepackage[bb=boondox]{mathalfa}
\usepackage{tikz}
\usetikzlibrary{matrix,arrows.meta,positioning}
% Define colors
\definecolor{olgreen}{HTML}{4f9c45}
% Microtype fix (keep as-is)
\makeatletter
\def\MT@is@composite#1#2\relax{%
  \ifx\\#2\\\else
    \expandafter\def\expandafter\MT@char\expandafter{\csname\expandafter
                    \string\csname\MT@encoding\endcsname
                    \MT@detokenize@n{#1}-\MT@detokenize@n{#2}\endcsname}%
    % 3 lines added:
    \ifx\UnicodeEncodingName\@undefined\else
      \expandafter\expandafter\expandafter\MT@is@uni@comp\MT@char\iffontchar\else\fi\relax
    \fi
    \expandafter\expandafter\expandafter\MT@is@letter\MT@char\relax\relax
    \ifnum\MT@char@ < \z@
      \ifMT@xunicode
        \edef\MT@char{\MT@exp@two@c\MT@strip@prefix\meaning\MT@char>\relax}%
          \expandafter\MT@exp@two@c\expandafter\MT@is@charx\expandafter
            \MT@char\MT@charxstring\relax\relax\relax\relax\relax
      \fi
    \fi
  \fi
}
\def\MT@is@uni@comp#1\iffontchar#2\else#3\fi\relax{%
  \ifx\\#2\\\else\edef\MT@char{\iffontchar#2\fi}\fi
}

\makeatother
% Geometry setup
\ifthenelse{\lengthtest { \paperwidth = 297cm}}
    { \geometry{margin=0.3in} }
	{\ifthenelse{ \lengthtest{ \paperwidth = 297mm}}
		{\geometry{top=0.5cm,left=1cm,right=1cm,bottom=0.5cm} }
		{\geometry{top=0.5cm,left=1cm,right=1cm,bottom=0.5cm} }
	}

% Page style setup
\pagestyle{empty}

% Section formatting
\makeatletter
\titleformat{\section}
{\ttfamily\large\bfseries}
{\thesection}{1pt}{}
\titlespacing*{\section}{0pt}{2pt}{-2pt}

\titleformat{\subsection}
{\normalsize\itshape\bfseries}
{\thesubsection}{1pt}{}
\titlespacing*{\subsection}{0pt}{2pt}{-2pt}

\titleformat{\subsubsection}
{\rmfamily\small\itshape}
{\thesubsubsection}{1em}{}
\titlespacing*{\subsubsection}{0pt}{2pt}{-2pt}

\makeatother
\newcommand\bigzero{\makebox(0,0){\text{\huge\itshape0}}}
% Paragraph settings
\setcounter{secnumdepth}{0}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt plus 0.5ex}
\everymath{\displaystyle}

% custom itemize

\setlist[itemize]{topsep=0pt, partopsep=0pt,itemsep=0pt}